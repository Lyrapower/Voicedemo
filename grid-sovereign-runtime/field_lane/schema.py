"""Compile artifact schema tags — chat-path vs /compile route."""

SCHEMA_STRICT = "schema:strict"
SCHEMA_LOOSE = "schema:loose"

COMPILE_SEMANTICS_STRICT = "strict"
COMPILE_SEMANTICS_PARSE_ONLY = "parse_only"

TEST_CONTENT_PREFIX = "[test] "
