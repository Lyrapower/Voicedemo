# Aster compile channel — local only (no external SSD paths).
# Override this file when you have your final Aster config.

[channel]
port = 8787
host = "127.0.0.1"
name = "Aster Compile"

[models.compile]
id = "mlx-community/Qwen2.5-1.5B-4bit"
lm_studio_base = "http://127.0.0.1:1234/v1"
conversation_id = "17797865158102"
context_length = 8192
system_prompt = "aster/prompts/compile_system.txt"

[models.daily]
id = "qwen/qwen3.5-9b"
# Separate LM Studio tab — not bound to Aster HTTP. Compile scripts never touch this tab.

[runtime]
substrate_backend = "lmstudio"
pythonpath = "repo"
