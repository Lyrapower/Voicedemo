# Cloud memory archive · 2026-08-05

Hot `messages` dumped then moved to `messages_archive` via store purge (hard=false).
NOT hard-deleted. Reason: cloud soul polluted with Grid/console content — seal until safer architecture.

Do not reconnect b11 Cloud / console 魂器 / soul-inject until Lyra unlocks.
