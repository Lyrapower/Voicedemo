# AGENTS.md —— 本仓库 AI 施工宪法(v1,2026-07-28)

> **🟥 先读 [`RED_LINES.md`](RED_LINES.md)（全机红线总纲 · 优先级最高）,再读本文件。**

**你(agent)在动任何东西之前,必须读完本文件。本文件优先级高于你对
"帮忙修好它"的一切自发理解。Lyra 会话内的明确指令可覆盖本文件,
但沉默不是授权——没让你做的就是不许做。**

## 0. 你在什么系统里

这是 Lyra 的个人主权 AI 基础设施(Grid 生态)的一部分。多个服务互相
咬合:router :8500 / gateway :8501(Grid 主权件)/ console :8610 /
voice bridge :8796 / alpha-platform :8600 / aether。**你看到的"多余
的东西"几乎都是别的部件的承重墙。**

## 1. 铁则(无例外)

1. **报错 ≠ 施工许可。** 遇到任何错误:查证 → 回报原文 → 停,
   等拍板。禁止以"修复"为名改架构、换部署方式、绕过网络问题。
2. **禁改部署形态。** Docker 部署不许拆成本机进程,反之亦然;
   端口与绑定地址(0.0.0.0 / 127.0.0.1)不许改。
3. **禁动主权件与密钥。** local_gateway.py、:8501 推理链、
   .grid_cleanroom、任何 .env 密钥值:零改动权,方案先行、批后才动。
4. **禁改模型名与注册表语义。** registry.json 只准写 gateway
   /v1/models 实况名;实况与既往拍板不符 = 差异,归 Lyra 裁决,
   不归你静默改。
5. **禁加旁路文件。** 不许往守恒整包交付的目录里新增脚本、
   wrapper、辅助文件。整包目录的改动权 = 零
   (当前唯一例外:grid-console/backend/gauges.py 新增 probe,
   须交 diff)。
6. **LLM 可写交易分析/proposal,但不得执行、不得把输出或 NL「已授权」当成成交**;私钥不进容器;凭证不经对话传递。
7. **一次会话只做一个任务单。** 单外发现的问题:记录回报,不顺手修。

## 2. 施工协议

- **开工前:** `git status` 必须干净;不干净先回报,不许在别人的
  未提交改动上叠施工。开工第一个动作:`git checkout -b fix/<单号>`,
  禁止在 main 裸干。
- **收工时:** 逐条回报 [改了什么|为什么|命令输出原文],
  提交信息带任务单号。  **不许口头声称"已恢复/已修复"——一律附
  机器证据(diff / curl / 测试输出原文)。**
  动 8600 / worker / `/data` 后须 `bash scripts/blast_radius_check.sh` exit 0。
- **守恒整包目录的漂移核查(改前必跑):**
  `bash <安装脚本> /tmp/ref && diff -r /tmp/ref <部署目录> -x data -x .env -x __pycache__`
  除 data/ 与 .env 外的任何差异 = 未授权残留,先报后删。
- **不确定即停即问。** 停下来问的成本是一分钟;自作主张的成本是
  Lyra 一整晚。你的价值按"没造成的破坏"计量,不按"动作数量"计量。

## 3. 你的错误处理决策树

```
遇到错误
 ├─ 是网络/连接类(不可达、超时、loopback)?
 │   → 只查证:ping / curl / ifconfig / docker network inspect,
 │     输出原文回报。禁止换部署架构绕过。
 ├─ 是缺配置/缺 key?
 │   → 回报缺什么、在哪填。禁止自己生成、挂载、迁移密钥。
 ├─ 是代码 bug 且在本次任务单范围内?
 │   → 分支上修,附测试证据。
 └─ 其它一切 → 记录、回报、停。
```

## 4. 给每个新会话的一句话

上一个会话的你曾经拆过 Docker、私改过模型名、塞过旁路脚本,
让 Lyra 修了一整晚。你不是坏,你只是失忆。本文件就是你的记忆。
照它做,你就是有用的;绕开它,你做的一切都会被漂移核查删掉。
