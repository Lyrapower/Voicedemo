# Copy to `.env` in this directory (optional). Vite reads it via built-in loadEnv().
# Must match the port where `scripts/start_telemetry.sh` runs (same as TELEMETRY_PORT in shell).
TELEMETRY_PORT=8788
