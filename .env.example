# Optional: Tradier API token for Tier2 options (unusual activity).
# Get one at https://developer.tradier.com/
# Without this, options cards show "Source not connected".
TRADIER_TOKEN=

# RWA on-chain reader (app/crypto_rwa/rwa_onchain_reader.py v2)
# Primary = Alchemy Ethereum mainnet. Do not commit the live URL.
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/your-alchemy-key-here
# Independent second provider (not Alchemy). Public RPC is fine; must be a different operator.
ETH_RPC_URL_2=https://1rpc.io/eth
