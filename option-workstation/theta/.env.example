# Theta Data · 凭证模板(复制为同目录 .env 后由你填;勿提交 .env)
# 优先 API key(dashboard 生成)。也可用邮箱密码二选一。

THETADATA_API_KEY=

# 若无 API key,改用账号(二选一即可):
# THETA_EMAIL=
# THETA_PASSWORD=

# Terminal HTTP(宿主机 / compose 内)
THETA_HTTP_PORT=25503
THETA_HTTP_HOST=0.0.0.0
