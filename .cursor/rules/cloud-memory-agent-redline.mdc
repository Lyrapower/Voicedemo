---
description: RED LINE — agent 禁止向 Cloud 生产 store 写验收数据（8501 cloud-glm52 / cloud-kimi）
alwaysApply: true
---

# Cloud tab 记忆 · agent RED LINE（2026-07-28）

**与 `workbench-b11-store-redline.mdc` 同级。** Cloud tab GLM/Kimi 记忆在 **8501 store**，不是 8515 sqlite。

**交叉升格（2026-08-02）：** `cloud-glm52` / `cloud-kimi` 正文适用同一条——epoch **只许 MOVE→`messages_archive`**，禁止硬删；详见 **`workbench-b11-store-redline.mdc`**。

## 生产 node（用户对话 · 禁止 agent 写验收）

| UI | 8501 store node |
|----|-----------------|
| GRID-GLM 5.2 | **`cloud-glm52`** |
| GLM 5.3 Flash | **`cloud-glm53`** |
| GLM 5.3 | **`cloud-glm53-full`** |
| Kimi K2.5 | **`cloud-kimi`** |

推理在 **8501 `POST /task/cloud_chat`**（多轮 messages）；**持久化**在 **8501 `/store/conversations/cloud-*`**。b11 Cloud **禁止**走 8515 `/cloud/chat` 或 `/cloud/memory`。

## GLM `empty_content` — 8501 永久策略（非 UI 一次性 retry）

逻辑在 **`code_task/cloud_chat_executor.py`** · `cloud_attempt_plan()` + `TRIM_PAIRS_ON_EMPTY`：

| 条件 | 8501 自动行为 |
|------|----------------|
| 记忆 ≥3 轮 **或** 正文 ≥4000 字 | **只用 `no_think`**（不开 think） |
| 短对话 | `think` → 空则 `no_think_fallback` |
| 仍空 | 裁最旧 0→2→4→6 条 user/assistant 再 `no_think` |
| 仍失败 | 502 + 明确 error；**store 不丢** |

**改 Cloud 推理/agent 前必读此文件。** 禁止在 b11 JS 里写「只 retry 一次」的临时逻辑替代服务端策略。

验收：`python3 -m unittest tests/test_cloud_chat_executor.py`

## 绝对禁止

1. **`POST .../cloud-glm52/messages`** 写 `u0/a0` 循环、ping、验收探测（同禁 **cloud-glm53** / **cloud-glm53-full** / **cloud-kimi**）
2. **8515 `POST /cloud/memory/glm52/turn`** 写生产 GLM 记忆（8515 层已废弃给用户路径）
3. 验收后留给用户删

## 合法验收

- **读**：`GET /store/conversations/cloud-glm52`
- **写验收**：临时 sqlite 或 **`smoke_node`**（若存在）；**不得** cloud-glm52 / cloud-glm53 / cloud-glm53-full / cloud-kimi

## 自检

- [ ] 未 POST 验收消息到 **cloud-glm52** / **cloud-glm53** / **cloud-glm53-full** / **cloud-kimi**
- [ ] 未宣称 Cloud 记忆在 8515（用户路径是 8501 store）
