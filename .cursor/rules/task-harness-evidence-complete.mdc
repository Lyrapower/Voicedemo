---
description: 禁止 one-shot 改完即报完成；必须自动自检 harness + 可证明证据后才能说完成
alwaysApply: true
---

# 任务完成 · 全自动 Harness（硬红线）

与 `verification-before-complete.mdc` 同级。**代码改完 / 口头“应该好了” ≠ 完成。**

## 绝对禁止

1. **One-shot simple fix**：改一两处 → 不跑检查 → 汇报「已完成 / 已修复 / 可以用了」
2. 用「逻辑上对了」「diff 看着对」「grep 有函数名」「health 字符串」代替端到端证据
3. 用户给了多条清单时，只做一部分就宣称整单完成
4. 发现失败后只报错停住、不进入「定位→修补→再检」循环（除非触红线必须停等拍板）

## 强制循环（每条任务都走）

```text
执行 → 自检 → 还有问题？潜藏漏洞？下次还会出现？
     → 清单每条都仔细执行了吗？
     → 否：发现问题 → 定位 → 修补 → 再检查
     → 是：证据齐全 → 才可汇报完成
```

自检必须主动回答：

| 问 | 要求 |
|----|------|
| 哪里还有问题？ | 列未过项；无则写「无，证据：…」 |
| 哪里有潜藏漏洞？ | 复发路径、静默失败、缺落地物、launchd 未装/未触发 |
| 下次是否还会出现？ | 有防护/门禁才算「不会」；否则继续修 |
| 给你的文件/条款是否每条执行？ | 逐条勾选；未做的不得藏 |

## 「完成」门槛（缺一不可）

1. **清单全覆盖**：用户每条要求有对应动作或明确阻塞原因  
2. **自动自检已跑**：脚本 / curl / launchd print / unittest / UI 亲测（按类型）  
3. **复发封堵**：同类失败有门禁、integrity、或硬断言；不只靠「下次注意」  
4. **可证明证据**：贴命令输出原文、路径、时间戳、exit code；禁止空口  

5. **波及面**：`bash scripts/blast_radius_check.sh` 须 exit 0。

未达门槛 → 只能说「部分完成 / 已改代码尚未验收」，并写下一步。

## 反例（等同再犯）

- Scout 改 token → 不查 `briefs/*-morning.json` / store emit → 说「晨会好了」
- 装 LaunchAgent → 不看 `launchctl print` runs/calendar → 说「定时会跑」
- 修灰卡 → 只造当日文件、不修 integrity → 次日同类再灰
- 修 8600 热力/锁库 → 不跑 `scripts/blast_radius_check.sh` → 扫描池过期仍说平台好了
