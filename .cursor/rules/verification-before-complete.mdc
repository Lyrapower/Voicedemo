---
description: 未自测跑通前禁止宣称已完成；附件/每条交付物须逐项验收
alwaysApply: true
---

# 完成声明 — 必须先验收跑通

> **升级（2026-08-12）：** 禁止 one-shot 改完即报完成。完整自检循环见同目录 **`task-harness-evidence-complete.mdc`**（alwaysApply · 硬红线）。
> **升级（2026-09-04）：** 动 8600 / worker / `/data` 后须 `bash scripts/blast_radius_check.sh` exit 0。

## 绝对禁止

在未亲自验证跑通之前，**禁止**对用户说：

- 「已完成 / 改好了 / 已修复 / 已验证 / 可以用了」
- 「验收通过 / 测试通过 / 本地已确认」
- 任何暗示任务已结束、用户可直接使用的表述

**代码改完 ≠ 完成。** 语法通过、逻辑看起来对、只读了 diff，都不算验收。  
**One-shot simple fix + 口头完成 = 违规**（须 harness 自检 + 证据）。

## 必须做的验收（按改动类型）

| 改动类型 | 最低验收 |
|----------|----------|
| UI / 前端 JS | 打开页面或浏览器自动化：关键按钮可点、无 Console 报错、主流程有可见结果 |
| HTTP API | 实际 `curl`/请求，核对状态码与响应体 |
| 脚本 / CLI | 运行命令，核对 exit code 与输出 |
| 定时任务 / launchd | 检查 load 状态；能 dry-run 则 dry-run |
| 附件 / 读图 / 多模态 | **每条附件路径单独测**：上传或等价请求，确认服务端识别、UI 展示、回复内容 |

## 多条交付物 — 逐条验收

用户任务含 N 项（功能点、文件、按钮、附件、端口）时：

1. 列出验收清单（ mentally 或简短写出）
2. **每一项**单独跑通后再勾选
3. 任一项未测 → 只能说「部分完成」并说明哪项未验、阻塞原因

**附件零容忍：** 声称「附件可用」前，须对每种附件类型（图片、粘贴、文件上传等）各跑至少一次端到端，不能只看代码路径。

## 允许的说法

- 「已改代码，尚未跑通验收 — 下一步：…」
- 「A、B 已 curl 通过；C（读图）未测，待你确认后补测」
- 「我这边浏览器发送失败，原因：…，尚未完成」

## 反例（勿再犯）

- 只删了 JS 语法错误、未测发送按钮 → **不能说**「Grid 已恢复」
- 只加了 `pendingBackendId` 声明、未点「发」→ **不能说**「已验证发送」
- 改了 3 个 UI 控件、只测了 1 个 → **不能说**「全部完成」
