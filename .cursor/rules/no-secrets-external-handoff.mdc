---
description: Never copy or send secret-bearing files to Fable, Desktop bundles, or any external handoff — examples and sanitized templates only.
alwaysApply: true
---

# 外发 / Fable / 桌面包 — 零密钥（NEVER）

## 绝对禁止

向外发、打包、复制给 **Fable**、**Desktop demo**、**deliver/**、邮件、CloudDocs 等任何「离开本机信任边界」的路径时：

1. **禁止**包含带真实密钥、token、密码、私钥的文件。
2. **禁止**复制生产 `.env`；只允许 `.env.example` / 脱敏模板。
3. **禁止** plist / json / toml / sh 里仍含 `sk-`、`api_key`、`TOKEN`、`SECRET`、`BRIDGE_STORE_TOKEN` 等占位已填真实值的文件。
4. **禁止** `diary.db`、`*token*.json`、`*credentials*`、LaunchAgent 日志里可能含 secret 的整包倾倒。
5. **禁止**「为方便调试」顺手塞 `.env` — **NEVER，无例外。**

## 唯一合法做法

| 需要传递的配置 | 怎么做 |
|----------------|--------|
| 环境变量说明 | 只发 `.env.example`，键名保留、值为空或 `your-*-here` |
| 脚本 / launchd | 发模板（`__HOME__` / `__DEMO_ROOT__` 占位），不发已 install 的本机 plist |
| 代码 | 纯 `.py` / `.html` / 无 secret 的文档 |
| 必须演示结构 | 手打 redacted 示例，不 copy 生产文件 |

## 打包前自检（agent 必做）

复制到 Desktop / `deliver/` / 给 Fable 前：

```bash
# 若命中任一模式 → 停止，换 example 或删文件
rg -l '(?i)(api[_-]?key|secret|token|password|sk-[a-z0-9]|BEGIN (RSA |OPENSSH )?PRIVATE)' "$DEST" 2>/dev/null
test ! -f "$DEST/.env" && test ! -f "$DEST/aether_nexus/.env"
```

- 命中 secret 模式 → **不得交付**，先剔除或脱敏。
- 不得对含 secret 的文件用「用户已批准」绕过本规则。

## 已犯过的错（勿再犯）

曾向 Desktop `Aether nexus/` 正确只用了 `env.example`；用户明确要求：**永远不要给 Fable 带密钥的文件 — NEVER。**
