# REGIME IC v4 · 问题 · 2026-09-05

**戌 → 溯 / Lyra**  
这些是拍板项，不是施工建议。沉默 ≠ 授权。

---

## 1. G5 校准失败（挡候选）

T10：139/500 至少一项 `regime_diff`，Wilson 下界 0.24 > 0.05。

问：下一包是 **改估计方法并新登记**（换带宽规则 / 联合检验形式 / 小样本处理——须预登记，不能看完结果再挑），还是 **接受「本方法在此场景下不能发候选」**、v4 到此为止？

---

## 2. `sp500_pit` 402

`historical-sp500-constituent` 仍 402。包禁止自动切 adv500。

问：Lyra 是否 **付费打开该端点**，还是宇宙维持 blocked？

---

## 3. adv500_pit（须 Lyra 一字）

可行性：**不具备**（没有 t−1 历史可投资候选全集、没有这些名字的 adv20 价量、没有上市/退市 + 稳定 ID）。当前库子集 **不得** 叫 `adv500_pit`。

问：要不要把「建 adv500 数据」开成另包？现在选它 = 仍 blocked。

---

## 4. 换码表

`symbol-change` n=100、日期 2026-06-24→2026-09-04。2023 起的更名没有来源。

问：另找稳定 ID 表（哪一家、是否付费），还是更名证券整段标 blocked？

---

## 5. 日线 src

`fmp_hist=0`；NULL 45.6 万行；fmp 6.1 万行且只在近端。R10 下 2023–2025 几乎空。

问：是否授权用 **已有 FMP 限速** 把 2023-01-03 起的日线写成 `src=fmp` 或 `fmp_hist`（不升档、不新钥）？还是承认研究窗不够、继续 blocked？

---

## 6. 价格口径

`historical-price-eod/full` 未见 adjClose。现标签 `price_return_ex_dividend`，开盘代理关闭。

问：是否要另采 FMP 分红复权 / 未拆股端点，才能打开开盘代理列？

---

## 7. ρ̂

T10 用的是此前 IC 序列的 ρ̂（mom_5=0.613 等），不是这次 fmp-only 2023 窗重估（窗本身 blocked）。

问：G5 重跑时，ρ̂ 是否必须先有合法 daily IC 再冻，还是允许继续用这组已登记值？

---

## 8. 下包边界

v1 说过「按状态挂门、不进排序键」。v4 明确 **regime_diff 只表示进秤候选，不挂门**。

问：在宇宙 + G5 解开之前，是否 **禁止** 任何热力/Scout 挂门施工？
