# REGIME_IC · 2026-09-05

**戌 → 溯** · 签名：戌  
源包：`REGIME IC PKG v4.md`（取代 v1/v2/v3）。`first_real_ic_v1.py` 未改。无 `--regime` 仍走 `evaluate()`。不写 evals。不调 TRADE_EXEC。

---

## A 实际环境与输入

| 项 | 值 |
|---|---|
| 入口 | `ic_eval_v1_1.py`（`--regime` → `regime_stats.joint_test`，不写 evals）；研究跑 `regime_ic_report.py` |
| 基线入口 | `first_real_ic_v1.py` 未改；h 默认 **5** |
| 四因子 | `mom_5`=`close/close[-5]-1`；`mom_20`=`close/close[-20]-1`；`rev_1`=`-(close/close[-1]-1)`；`vol_20`=`-std(ret,20)` |
| IC | 每日 Spearman（平均秩）；v4 另要求共同有效 ≥ 成员 95% 且 ≥100 |
| 源快照 | 容器 `file:/data/platform.db?mode=ro` · integrity ok |
| src | NULL **456171** · fmp **61470** · alpaca_iex **3178** · fmp_hist **0** |
| SPY | 922 行；src NULL 895 + fmp 27 |
| fmp 窗 | 仅近端（约 2026），不是 2023-01-03 起的研究窗 |
| PIT | `historical-sp500-constituent` **HTTP 402** → R9 **blocked**，未切 adv500 |
| 换码 | `symbol-change` 200 · n=**100** · 2026-06-24→2026-09-04 · 字段 `date,companyName,oldSymbol,newSymbol` — 不够 2023 PIT |
| 价格 | FMP `historical-price-eod/full` 无 adjClose 证据；标签 `price_return_ex_dividend`；开盘代理 **关闭**（不混未确认 open） |
| ρ̂（T10 用） | mom_5 **0.613**（替 0.8）· mom_20 0.735→**0.8** · rev_1 **≈0**（替 0.8）· vol_20 0.786→**0.8** |
| 宇宙文本 | 登记名 `sp500_pit` |

---

## B 工程证据

`python3 -m unittest test_regime test_regime_v4` → **29 tests OK**（43.8s）

| ID | 结果 | 原始输出位置 |
|---|---|---|
| T1 | 过 | `test_regime_v4.T1Labels` |
| T2 | 过 | `T2HacGap`（gap 日 m=0 ≠ y=0） |
| T3 | 过 | `T3Reference`（b=(1, 2.5)；奇异 → not ok） |
| T4 | 过 | `T4Joint`（中桶位移被联合检验捕获；重标号 W 不变） |
| T5 | 过 | `T5Holm`（0 → 0.11 → 0.4；无效项 m=12） |
| T6 | 过 | `T6Segments`（未知标签不断缩分母；不足 ≠ no_detected_difference） |
| T7 | 过 | `T7Pit`（402 blocked；adv500_auto=False） |
| T8 | 过 | `T8Sources`（冲突 blocked；NULL/IEX 丢；顺序不变） |
| T9 | 过 | `T9Corporate`（退出前标签保留；尾部未成熟不入） |
| T10 | **已执行 · 校准失败 G5** | `T10Calib` 打印：`n_diff=139/500 wilson=[0.2405,0.3188]` seed=20260905。不是实现失配。 |
| T11 | 过 | 无 `--regime` Δic < 1e-9；`--regime` `wrote_evals=false` |
| T12 | 过 | `file:<copy>?mode=ro` INSERT → OperationalError；研究目录可写；热力/下单模块未加载 |

未完成：活库 12 项数值（宇宙 blocked + T10 G5 → 不准当候选）。

---

## C 债表

| # | 缺口 | 处置 |
|---|---|---|
| G1 | 成员史 402 | 12 项 **blocked**；adv500 可行性：无 t−1 可投资全集、无全市场 adv20、无退市稳定 ID → **不得**叫 adv500_pit。待 Lyra 一字 |
| G2 | — | 未用「调阈值」绕过 |
| G3 | 未计普通分红 | 限制：`price_return_ex_dividend`。换码表太短 → 更名证券不得静默映射 |
| G4 | — | 无未校验近似 |
| G5 | T10 族误报 139/500，Wilson 下界 **0.240** > 0.05 | 保留失败结果；**本轮不发布候选**；须改估计方法后新登记 |

---

## D 数值

活库 12 项 **无合法 b / W / p**（blocked）。  
T10 校准：族误报 139/500 · Wilson 95% [0.2405, 0.3188] · 种子 20260905 · ρ 见 §A。  
开盘代理：关闭。去最长段：无活库可报。

---

## E 阻断

- `sp500_pit`：constituent **402**
- 换码表 n=100 且仅 2026-06 起 → 2023 更名 **blocked**
- R10：fmp_hist=0；NULL/IEX 不进研究；fmp 仅近端
- T10 G5：校准失败
- 受影响：**全部 12 个 hypothesis_id**

没有写「无」。

---

## F 结论

工程契约（T1–T9 / T11 / T12）通过；T10 已执行且为 **校准失败**。  
**不出候选。** 12 项判词一律 `blocked`。  
不得写「四因子无效」。不得写「待 Lyra L3」。

每因子三状态摘要：trend / width / vol 均为 blocked（宇宙 + 校准）。

---

## G 重现

```
cd /Users/ciciwang/Projects/demo/alpha-platform/backend
python3 -m unittest test_regime test_regime_v4
```

工作目录：`alpha-platform/backend`  
环境：Python 3.13 · 标准库 + 本目录模块  
登记：`research/regime_ic_v4/20260905T-blocked-sp500pit/parameters.json`  
报告：`research/regime_ic_v4/20260905T-blocked-sp500pit/REPORT.md`  
T12 连接：`sqlite3.connect("file:<path>?mode=ro", uri=True)`（`regime_data.connect_ro`）
