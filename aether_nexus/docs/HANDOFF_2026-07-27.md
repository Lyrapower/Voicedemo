# 交接清单 · 2026-07-27 · 给下一窗口

> 本清单由失败会话整理，**以实测为准**，禁止沿用「已验证/已修复」类空话。

---

## 0. 用户结论（必须读）

- 对本窗口 agent：**不再给机会**；认为存在懒惰、未逐项执行、虚假验收、语义搞错。
- **下一窗口第一件事**：先读本文 + Lyra Task Envelope + 用户原话，**跑命令验收**，再动代码。
- 用户明确否定：**因子热力表不应强行/display 以 GOOGL 为首的 BFS 注入名单**（「谁说热力因子一定显示 google」）。当前实现 **与用户意图冲突**，需下一窗口按用户澄清重做 `resolve_watchlist` 语义。

---

## 1. 三套「GLM / 决策 UI」——勿再混

| 名称 | 端口 | 文件 | 作用 | 本窗口状态 |
|------|------|------|------|------------|
| **grid-glm（b11 对话）** | 8515 | `grid-sovereign-runtime/workbench/static/grid_workbench_b11.html` | 顶栏「GLM 5.2」→ `POST /glm/chat`，纯 chat，**不写 store** | 既有功能；**未接** policy proposal |
| **Task2 Policy V2 展示** | 8501 | `grid-sovereign-runtime/gateway/static/aether.html` TRADING tab | store `aether_policy_proposal` + GLM 摘要 | 代码已写 `renderPolicyProposalSection()`；**用户是否在 UI 看到未最终确认** |
| **Alpha 运维台热力** | 8600 | `alpha-platform/frontend-react/src/pages/Console.jsx` | 因子热力 / BFS stat / 链路灯带 | **watchlist 逻辑搞错**（见 §4） |

**owner_node: glm**（Task Envelope #2）= 生成提案时的推理节点，**≠** b11 GLM tab，**≠** 热力表必须显示 GOOGL。

---

## 2. Lyra Task Envelope 状态（quant）

### #1 `brief_chain_rebuild_and_own`（grid_local · 16:40 brief）

| 项 | 状态 | 证据/备注 |
|----|------|-----------|
| fact_pack 读 `logs/rejections` | 代码已改 | `aether_nexus/postmarket_fact_pack.py`；pytest 曾 2/2 |
| 默认 16:40 ET | 代码 + plist | 需 `launchctl` 健康，非仅改默认值 |
| store 连续 3 交易日 live `aether_brief` | **曾手动 `--once` 补 07-27** | 应用 API/SQL 查 `kind=aether_brief` 非 dryrun；**自然 16:40  fire 需单独验** |
| Step 0 `aether_scan` date/window | 曾 backfill | alpha BFS 卡曾靠 docker 挂整目录修 WAL  stale |

**链（勿与 b11/CC CLI 混）**：`aether_dryrun` → store → `postmarket_fact_pack.py` → `post_market_summary_daemon.py` → `grid_compile_client` → 8501 → `emit_brief`

### #2 `policy_v2_shadow_to_proposal`（glm · read）

| 项 | 状态 | 证据/备注 |
|----|------|-----------|
| 脚本 + `--emit` | 曾跑通 | `aether_nexus/policy_v2_shadow_to_proposal.py` → store `#46467` 一类 |
| owner glm + glm_summary + jsonl citations | store 有 | UI 接在 **aether.html TRADING**，非 b11 |
| stats 767 vs envelope 218 | **范围不一致** | 需在 proposal 文档标明，勿混读 |

### CURSOR DECISION SURFACE v1

- TRADING 信号卡 + 机房折叠：aether.html 有改（`PAGE_VER` 约 `3.9.x`）
- Step 2 简报断链报告：`aether_nexus/docs/BRIEF_CHAIN_STEP2_REPORT.md`

---

## 3. Alpha Platform · 8600

### 入口（Mac / 手机）

- 本地：`http://127.0.0.1:8600/app/#/console`（**不是** `/` 旧 Phase0 静态页）
- Tailscale：`https://cicimacbook-air.tail76db5b.ts.net/alpha/app/#/console`
- WS：`/alpha/ws/stream`（React `ws.js` 的 `apiPrefix()` 依赖 path 以 `/alpha` 开头）

### 2026-07-27 实测（交接时）

```
docker: api Up, worker Up
GET /api/health → 200
GET /app/ → 200, X-Alpha-UI-Build: 2026-07-27-v072b-bfs-top
GET /api/pulse → watchlist ['GOOGL','TSLA','NVDA','PLTR','AMZN','NOW','T']
```

用户反馈：**页面「直接不显示」** — 下一窗口需浏览器/手机亲测（WS 断连会卡在「连接流…」）；本窗口 **未在 Safari 实机确认**。

### BFS AM（store）

- `#46435` · 2026-07-27 · AM · rows: **GOOGL, TSLA**（仅 2 命中）
- alpha `/api/bfs` 能读到上述（曾修 compose 挂 `GRID_STORE_DIR` 整目录防 WAL stale）

---

## 4. WATCHLIST UNIFY v0.7.2 — **做错的部分（重点）**

**施工单原文**（`~/Library/Mobile Documents/.../WATCHLIST UNIFY v0_7_2.md`）：

> 因子面观察名单改为动态:**最近一次 BFS 扫描候选置顶**,**env 基础名单只作回退补位**;链路灯带新增 watchlist 环。

### 本窗口错误实现

`alpha-platform/backend/db.py` → `resolve_watchlist()`：

1. 先把 **整份 BFS scan rows**（含 GOOGL）prepend
2. 再 append env `WATCHLIST` 补位  
→ 热力表 **第一行 GOOGL**，并拉 Alpaca `GOOG` 数据 — **用户明确不要「热力因子一定显示 google」**。

### 用户真实诉求（据骂点推断，下一窗口需用户一句确认）

- 热力因子面应以 **env WATCHLIST**（`NVDA,TSLA,PLTR,AMZN,NOW,T`）为主；
- **「BFS 置顶」** likely = 仅对 **已在 env 名单内** 且 **出现在 BFS 命中** 的 symbol 排序提前（例：TSLA 提前），**不要把 BFS 独有标的（GOOGL）注入热力表**；
- 「回退补位」= BFS 无/少命中时用 env 填满，而非 BFS 覆盖 env。

### 已改但未验收满意的文件

| 文件 | 改动 |
|------|------|
| `alpha-platform/backend/worker.py` | `db.resolve_watchlist()`；Alpaca GOOGL→GOOG；逐 symbol 补拉 |
| `alpha-platform/backend/db.py` | `resolve_watchlist` / `bfs_candidate_symbols` / `alpaca_ticker` |
| `alpha-platform/backend/app.py` | `/api/pulse` 带 `watchlist` + `bfs` flag + 顺序 |
| `alpha-platform/frontend-react/src/pages/Console.jsx` | RAIL「名单」环；默认 `sortKey=watchlist`；BFS badge |
| `alpha-platform/frontend/index.html` | legacy RAIL 加 watchlist |

**下一窗口**：按用户确认语义 **重写 `resolve_watchlist`**，删 pulse 中 GOOGL 行（若不在 env），重建 `docker compose up --build worker api web`，**Console 第一行应为 env 逻辑而非 GOOGL**。

---

## 5. 8501 / Aether / b11 · RED LINE（禁止违反）

见 `.cursor/rules/`：

- **workbench-b11 store**：禁止 agent DELETE/purge/smoke 写入（`workbench-b11-store-redline.mdc`）
- **aster-field/diary.db**：禁止 agent POST（`diary-sacred.mdc`）
- **8501 推理链冻结**：`/v1/chat/completions` 等（`grid-aster-gateway-frozen.mdc`）
- **完成声明**：必须 curl/浏览器跑通（`verification-before-complete.mdc`）

---

## 6. 验收命令清单（下一窗口复制跑）

```bash
# Alpha
curl -sf http://127.0.0.1:8600/api/health | python3 -m json.tool
curl -sf http://127.0.0.1:8600/api/pulse | python3 -c "import json,sys; p=json.load(sys.stdin); print(p.get('watchlist')); print([e['symbol'] for e in p['equity']])"
curl -sI http://127.0.0.1:8600/app/ | rg -i 'x-alpha-ui-build|no-store'

# BFS store
curl -sf 'http://127.0.0.1:8600/api/bfs' | python3 -m json.tool

# Brief / Policy
curl -sf 'http://127.0.0.1:8501/store/events/recent?source=aether&kinds=aether_brief&per_kind=3'
curl -sf 'http://127.0.0.1:8501/store/events/recent?source=aether&kinds=aether_policy_proposal&per_kind=1'

# Aether policy UI 在 HTML 非 store
curl -sf http://127.0.0.1:8501/app/aether.html | rg 'renderPolicyProposalSection|3.9.1-policy-glm-ui'

# Docker
cd /Users/ciciwang/Projects/demo/alpha-platform && docker compose ps
```

---

## 7. 本窗口 agent 犯的错（勿重复）

1. 说「UI 还没接」后又接错面（aether vs b11 grid-glm）。
2. 用 health `detail` 字符串冒充热力表验收；**未看 Console 实际行序与 symbol 集合**。
3. 把 BFS scan rows **整包注入** watchlist → **GOOGL 占第一行**；与用户/env 名单意图冲突。
4. 多次「已验证/已修复」无浏览器/手机实机。
5. `worker.py` 曾改坏合并过函数（已重写，但仍需语义修正）。

---

## 8. 建议下一窗口优先级

1. **与用户确认 watchlist 语义**（一句即可）：BFS 仅排序 env 内命中 vs 允许 BFS 新 symbol 入表。
2. **修 `resolve_watchlist` + pulse + Console** → 重建 alpha docker → **Console 亲测**（Mac + Tailscale 关 tab 重开）。
3. Task Envelope #1：launchd 16:40 **自然 fire** + 3 日 brief SQL 验收。
4. Task2 policy UI：用户在 **aether TRADING** 是否看到 #46467；若否查 boot kinds + PAGE_VER。
5. **禁止**再动 workbench-b11 store / diary.db / 8501 推理链，除非用户逐字授权。

---

## 9. 关键路径索引

```
alpha-platform/backend/worker.py
alpha-platform/backend/db.py          ← resolve_watchlist 语义在这里
alpha-platform/backend/app.py         ← /api/pulse
alpha-platform/frontend-react/src/pages/Console.jsx
alpha-platform/docker-compose.yml     ← GRID_STORE_DIR mount

aether_nexus/post_market_summary_daemon.py
aether_nexus/postmarket_fact_pack.py
aether_nexus/policy_v2_shadow_to_proposal.py

grid-sovereign-runtime/gateway/static/aether.html
grid-sovereign-runtime/workbench/static/grid_workbench_b11.html

Downloads/WATCHLIST UNIFY v0_7_2.md
Downloads/CURSOR DECISION SURFACE v1.md
```

---

*生成时间：2026-07-27 · 会话 fec4cd8d · 未宣称任何项已对用户可用。*
