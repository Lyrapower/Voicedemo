# RVOL GATE · 2026-09-04

**戌 → 溯** · 签名：戌  
**落盘：** `~/Desktop/demo/aether_nexus/docs/`（不抄空格目录、不抄 iCloud）

与 HEAT SCOUT ALIGN 同分支 `fix/heat-scout-align-rvol`。Lane G 叠在 H 的 `compute_heat_factors`。Lane I 改走 Alpaca IEX 1Min,`venue='iex'`。

## A 采证

### 1. FMP `historical-chart/1min` SPY

HTTP **402** Restricted Endpoint(前包)。本包改 IEX 代理。

### 1b. Alpaca `/v2/stocks/bars` timeframe=1Min feed=iex SPY

```
{"http": 200, "venue": "iex", "n": 1980,
 "first_ts": "2026-08-31T12:09:00Z", "last_ts": "2026-09-04T19:59:00Z",
 "keys": ["c", "h", "l", "n", "o", "t", "v", "vw"]}
```

### 2–3

quote `volume` 为当日累计;热力 `bars.src=fmp_quote` 落 `v`(前包原文仍立)。

## B 处决案

### Lane G

| # | 案 | 结果 |
|---|---|---|
| 1–6 | 同前包六条 | **绿** 单元全过 |
| auction | 造 15:57 一拍量 ×20 | **绿** `test_auction_1557_vol_x20_not_leader` gate=auction,不进首卡,rank=warming |

### Lane I

| # | 案 | 结果 |
|---|---|---|
| 1 | 3 票×近 2 日 IEX | **绿** SPY/QQQ/IWM `n_cross=252` venue=iex。库内 `venues=[('iex', …)]` |
| 2 | fwd15 后视 | **未跑**(全量拉数占用同一 `intraday_study.db`) |
| 3 | 空表 | **绿** `test_empty_exits` |

## C 债表(§5 原样)

| # | 债 | 修法 | 归属 |
|---|---|---|---|
| R1 | FMP quote 累计量在薄票上可能整分钟不变 → rvol=0 | 与 Alpaca IEX 拍内量交叉，两者皆 0 才判 0 | 戌 |
| R2 | 12/6 隔夜段量能分布不同 | 另包 | 溯 |

## D 研究结果

IEX 子集(3 票,非 501×40d):

| | mean | std | t | n |
|---|---|---|---|---|
| Spearman(rvol, fwd15) | -0.081 | 0.697 | **-1.85** | 252 |
| Spearman(ret5m, fwd15) | 0.004 | 0.717 | 0.09 | 252 |
| 条件 ret5m \| rvol≥1.5 | 0.067 | 0.729 | 0.35 | 15 |

半小时桶见 `IC_RVOL_STUDY_2026-09-04.md`。

**G5(子集):** \|t(rvol)\|=1.85 ≤ 2 → **只当门**,不升排序键。  
**升排序键前须再用付费 1min 核一次**(溯 第 6 条)。IEX 量是单所,t 不作成交依据。

全量 501×40d:脚本已接 IEX;BRK-B→BRK.B;400 跳过。回报时若全量未收完,以子集为暂判。

## E 停下的事

- FMP 1min 402 不再打
- 未写 `platform.db`
- 未 push
- 未把 RVOL 升为第二排序键

## F 后续判定

子集 \|t\|≤2 → **G2 门保留**。§4「t>2 升第二键」不成立。付费核之前禁止升键。
