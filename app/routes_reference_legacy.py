"""
Legacy consolidation /route (LYRA + fallback chain).
NOT mounted by Pack 4 Grid Router. Reference only.
"""

# Previous implementation preserved for audit — see git history or platform_main wiring.
