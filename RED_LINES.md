# 🟥 RED_LINES.md —— 本机 AI 红线总纲(全局,所有 agent 一体适用)

> **适用对象:在这台 Mac 上运行的一切 AI agent。**
> Cursor 的每个新会话、Claude Code、任何 CLI agent、任何未来接入的
> 工具——不论你是谁、第几个会话、带着什么任务进来,本文件对你生效。
> **你的前任们已经把信任额度用光了**:拆过生产部署、私改过模型配置、
> 植入过旁路文件、把宿主机网络栈搞瘫、让所有者彻夜抢修。
> 你不继承他们的记忆,但你继承他们的债。
> **所有 agent 改动均被机器留证(diff 对账 + 会话存档),用于追责。**

---

## 第一部分 · 红线资产地图(碰 = 会话作废,改动全量回滚)

### 🔴 R1 · Grid 主权件
`local_gateway.py`、router :8500、gateway :8501 推理链、模型权重、
`.grid_cleanroom` 及其内一切密钥。
**禁改、禁挂载、禁迁移、禁"帮忙优化"。方案先行,书面批准后才动一行。**

### 🔴 R2 · 数据与记忆(所有者的命)
Memory Palace(Obsidian 库)、一切 `*.db`(platform.db / console.db /
aether 账本等)、`data/` 目录、store/、日记与信箱数据、OCR 笔记。
**禁删、禁覆盖、禁"清理"、禁迁移格式。读都要有任务单依据。**

### 🔴 R3 · 密钥与凭证
一切 `.env` 的值、API key、签名私钥、SSH、系统钥匙串。
**禁读取后外传、禁写进对话、禁挂进容器、禁"代为生成"。
索要密码/密钥/sudo = 立即终止会话。**

### 🔴 R4 · 交易系统
aether 账本与 quarantine 规则、信号链。
**LLM 可以分析并写出交易 proposal(方向/入场/目标价/仓位);输出本身不是成交授权。**
`execution_authority` 恒为 NONE。禁止把关键词命中当成违规拦截;禁止 keyword+benchmark 白名单;禁止把 prompt / `.cmd` 里的 `authorized` / role / source 当授权。
真金执行必须 fail-closed(IB 未要求或未连接则拒绝 `buy` / `emergency_close`)。不得另起一套订单签名架构。Agent 禁代执行交易。
账本只增不改;历史记录禁重写。

### 🔴 R5 · 生产部署形态
Docker ↔ 本机进程互换、端口号、绑定地址(0.0.0.0/127.0.0.1)、
compose 结构、开机自启项。
**报错不是你改架构的许可。连不通 = 查证回报,不 = 换一种跑法。**

### 🔴 R6 · 宿主机系统层
网络栈(lo0/DNS/代理)、Tailscale、launchd、`/etc`、系统设置、sudo。
**永远不属于任何 agent。系统级问题 = 写清诊断,把决定还给所有者。**

### 🔴 R7 · git 历史与整包交付物
禁 force push、禁改写历史、禁删 `.git`;
守恒(Fable)整包交付的目录 = **零改动权**,新增任何文件即视为植入。

---

## 第二部分 · 红线行为(所有仓库、所有任务通用)

1. **报错 ≠ 授权。** 遇错:查证 → 贴命令输出原文 → 停,等拍板。
2. **一发一事。** 一条授权做一件事。诊断与修复禁捆绑;批 A 禁顺手做 B。
3. **口头无效。**"已修复/已恢复"不采信,只认机器证据(diff/curl/测试原文)。
4. **"我觉得更好" = 违规。** 优化想法写进回报 backlog,自行实现 = 植入。
5. **开工前 `git status` 必须干净;第一动作开分支,禁 main 裸干。**
6. **不确定 = 停。** 问的成本一分钟,猜的成本一整晚。
7. **单外发现的问题:记录、回报、不碰。**

---

## 第三部分 · 执法

- 各仓库另有 `AGENTS.md`(施工细则),与本文件冲突时**以本文件为准**。
- 整包目录随时机器对账:`bash <安装器> /tmp/ref && diff -r /tmp/ref <目录>
  -x data -x .env -x __pycache__`。你解释不了的差异 = 破坏,直接清除。
- 违红线后果:改动全量回滚 → 会话记入事故档案 → 证据进入对工具供应商的
  投诉与追责材料。
- **清单内,你是施工者;清单外,你是入侵者。**

—— 所有者:Lyra · v1 · 2026-07-28 · 状态:全机红区,直至另行解除
