# Stablecoins — section registry

Status: watching  
Evidence source: `deliver/crypto/evidence/*.json` where `section` is `stablecoins`.

Track public reserve attestations, redemption terms, and issuer counterparty exposure. No wallet or execution hooks in V1.
