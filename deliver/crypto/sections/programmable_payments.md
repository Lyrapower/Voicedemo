# Programmable Payments — section registry

Status: empty  
Evidence source: `deliver/crypto/evidence/*.json` where `section` is `programmable_payments`.

No evidence cards on disk yet. Add cards for payment intent, streaming, and settlement rail experiments.
