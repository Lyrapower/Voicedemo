# AI-Agent Financial Rails — section registry

Status: empty  
Evidence source: `deliver/crypto/evidence/*.json` where `section` is `ai_agent_rails`.

No evidence cards on disk yet. Add cards for agent request/settle rails that require human approval gates.

Reference artifact: `deliver/crypto/onchain/BOUNDARIES.md`
