# On-chain Identity — section registry

Status: empty  
Evidence source: `deliver/crypto/evidence/*.json` where `section` is `onchain_identity`.

No evidence cards on disk yet. Add JSON cards for attestation and credential protocols when identified.
