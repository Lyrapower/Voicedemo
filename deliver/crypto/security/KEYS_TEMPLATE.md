# Client Key Checklist (V1)
- Exchange API keys: READ-ONLY unless absolutely required
- Withdrawals: disabled if platform supports it
- IP allowlist: enabled (if platform supports)
- 2FA: required
- Keys stored in OS keychain / password manager (not in repo)
- No keys in .env committed to git
- No keys pasted into logs / tickets / notes
